# pgvector-patroni-oci

OCI image build for PostgreSQL with pgvector and Patroni.

The image is based on the official pgvector PostgreSQL image and adds
a clean, isolated Patroni runtime environment.

This repository contains image build and release artifacts only.
Docker Compose files and deployment-specific configuration are maintained
separately.
